- name: Config server and run app
  hosts: web_servers
  gather_facts: no
  tasks:
    - name: Update apt and install Nginx
      raw: apt-get update && apt-get install -y nginx
    
    - name: Ensure Nginx is running
      raw: service nginx start
