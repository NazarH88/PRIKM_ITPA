---
- name: Emergency Setup
  hosts: all
  gather_facts: no
  tasks:
    - name: Install Python 3
      raw: apt-get update && apt-get install -y python3
      changed_when: false

    - name: Install Nginx
      apt:
        name: nginx
        state: present
        update_cache: yes
