---
- name: Deploy Containerized App
  hosts: servers
  tasks:
    - name: Create a deployment log
      shell: "echo 'Deployment successful on $(date)' > /tmp/deploy_status.txt"

    - name: Print confirmation
      debug:
        msg: "Infrastructure is managed by Terraform, Ansible checked the connection."
